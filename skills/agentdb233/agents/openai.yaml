display_name: agentdb233
short_description: Shared project brain for Codex, Claude, Cline, OpenCode, and other AI agents.
default_prompt: Use agentdb233 as the shared external brain. Build or refresh the project index, import shared norms, then retrieve a compact context pack instead of reading full project docs.
